name: groubuy
description: A new way to buy.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  #UI
  cupertino_icons: ^0.1.3
  fluttertoast:
  cached_network_image:
  carousel_slider: ^2.3.1
  percent_indicator: ^2.1.8
  custom_navigation_bar: ^0.7.1
  #Database
  cloud_firestore:
  firebase_core:
  firebase_storage: ^3.0.6
  #Misc
  file_picker: 2.0.0
  random_string: ^2.0.1
  get_it: ^3.0.1
  provider: ^4.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - assets/images/

  uses-material-design: true

